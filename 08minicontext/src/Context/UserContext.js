import React from "react";

const UserContext = React.createContext();

export default UserContext;

// now create a provider component which will wrap the entire 
// application and provide the data to all the components in the application

